% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/space_xfplate.R
\name{plot_bioenergetic_space}
\alias{plot_bioenergetic_space}
\title{Plot Bioenergetic Space for Each Group}
\usage{
plot_bioenergetic_space(
  df,
  ecar_title = "J ATP glycolysis (pmol/min/ug)",
  ocr_title = "J ATP oxphos (pmol/min/ug)",
  legend_title = "Group",
  palette = NULL,
  title = NULL
)
}
\arguments{
\item{df}{A tibble returned by `calculate_space()`. Must include columns:
`group`, `basal_ocr`, `fccp_ocr`, `basal_ecar`, `amrot_ecar`.}

\item{ecar_title}{Label for the x-axis (ECAR). Default: `"ECAR (pmol ATP/min/ug)"`.}

\item{ocr_title}{Label for the y-axis (OCR). Default: `"OCR (pmol ATP/min/ug)"`.}

\item{legend_title}{Optional title for the legend. Default: "Group".}

\item{palette}{Optional named vector of fill colors (e.g., group names as names).}

\item{title}{Optional plot title (default `NULL`).}
}
\value{
A `ggplot` object
}
\description{
Visualizes the metabolic space for each group using rectangles, basal points,
and maximal capacity for both ECAR and OCR axes.
This function expects output from `calculate_space()`.
}
\examples{
# Simulate output from calculate_space()
df_space <- tibble::tibble(
  group = c("control", "treated"),
  basal_ocr = c(25, 30),
  fccp_ocr = c(50, 60),
  basal_ecar = c(20, 28),
  amrot_ecar = c(40, 42)
)

# Define group colors
palette <- c("control" = "#1b9e77", "treated" = "#d95f02")

# Plot bioenergetic space
plot_bioenergetic_space(
  df = df_space,
  ecar_title = "J ATP glycolysis (pmol/min/ug)",
  ocr_title = "J ATP oxphos (pmol/min/ug)",
  palette = palette,
  title = "Example Bioenergetic Profiles"
)
}
