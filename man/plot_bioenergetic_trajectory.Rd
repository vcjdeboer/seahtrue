% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/space_xfplate.R
\name{plot_bioenergetic_trajectory}
\alias{plot_bioenergetic_trajectory}
\title{Plot Metabolic Trajectories in J-space}
\usage{
plot_bioenergetic_trajectory(
  df,
  palette = NULL,
  title = "Metabolic J-space",
  label_map = c(Baseline = "Basal", FCCP = "FCCP", `AM/Rot` = "AM/Rot")
)
}
\arguments{
\item{df}{A tibble as returned by `calculate_space()`. Must contain columns:
`group`, `basal_ocr`, `fccp_ocr`, `amrot_ocr`, `basal_ecar`, `fccp_ecar`, `amrot_ecar`.}

\item{palette}{Optional named vector of fill colors (names = groups).}

\item{title}{Optional title for the plot. Default: `"Metabolic J-space"`.}

\item{label_map}{Optional named vector to rename default state labels (e.g., `c("Baseline" = "Basal")`).}
}
\value{
A `ggplot` object showing OCR vs ECAR trajectories with labeled points and arrows.
}
\description{
Converts the wide-format `df_space` tibble into a trajectory plot showing the progression
of metabolic states (e.g., Baseline -> FCCP -> AM/Rot) for each group in OCR/ECAR space.
Arrows indicate direction of metabolic shifts across conditions.
}
\details{
The final (AM/Rot) point of each trajectory is plotted at `amrot_ecar`
(the antimycin A / rotenone ECAR), which represents the glycolytic
capacity reached after mitochondrial inhibition. This is by design and
differs from the `max_ecar` value used inside [calculate_space()] for
the `supply_index` and `glyco_index_max` metrics, where `max_ecar` is
taken from `mon_ecar` (or `om_ecar`). The plotted trajectory and those
metrics therefore intentionally reflect different injection points.
}
\examples{
df_space <- tibble::tibble(
  group = c("control", "treated"),
  basal_ocr = c(25, 30), fccp_ocr = c(50, 60), amrot_ocr = c(15, 18),
  basal_ecar = c(20, 28), fccp_ecar = c(22, 32), amrot_ecar = c(18, 21)
)
palette <- c("control" = "#1f78b4", "treated" = "#33a02c")
plot_bioenergetic_trajectory(df_space, palette = palette)
}
